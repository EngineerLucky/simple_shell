#include <stdio.h>

/**
 * main - The entry point of the program
 * Return: Always 0 (Success)
 */
int main(void)
{
	int v;

	printf("Hello, Lucky!\n");

	for (v = 0; v < 10; v++)
	{
		printf("Iteration %d\n", v);
	}

	return (0);
}
